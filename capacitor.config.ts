import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xscientist.in',
  appName: 'kinder-games',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
